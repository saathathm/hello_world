name = "hello-world"
type = "javascript"

[build]
command = "npm install"

[env.production]
route = "https://hello-world-12y.pages.dev/*"  # Use Cloudflare Pages URL for now
zone_id = "your-zone-id"  # You can leave this for now if you don’t have a custom domain
account_id = "your-account-id"